# ── Required: Claude parses natural-language commands into task actions ─────────
# WhisperFlow handles speech-to-text system-wide — just focus the command bar and speak.
VITE_ANTHROPIC_API_KEY=sk-ant-...

# ── Optional: cross-device real-time sync (Supabase deploy step) ───────────────
# VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
